<?php

	/**
	 * 跨平台
	 * 安全性好，效率高
	 * 成本低
	 * 开放源码
	 * 面向对象
	 *
	 * 安装复杂
	 * 解释执行，不需编译，错误在运行时出现
	 *
	 * 服务器端语言
	 *
	 */

	/**
	 *
	 *
	 * B/S与C/S比较的优缺点：
	 * 	优点：
	 * 		开发成本低
	 * 		管理维护简单
	 * 		产品升级便利
	 * 		对用户的培训费用低
	 * 		用户使用出现故障的概率小
	 * 	缺点：
	 * 		安全性不足
	 * 		客户端不能随心变化，受浏览器限制
	 *
	 */

/*
	echo "PHP_INT_SIZE显示一个整数用多少字节: ".PHP_INT_SIZE;
	echo "<br/>";
	echo PHP_INT_MAX;
?
	echo "<br/>";
*/

	// $val = "world";
	// $str1 = "Hello, $val";
	// $str2 = 'Hello, $val';
	// echo $str1 . "<br/>";
	// echo $str2;

	$a = false || true;		//result = false || true, $a = result
	$b = false or true; 	// $b = fasle, $b or true
	var_dump($a, $b);

 ?>
